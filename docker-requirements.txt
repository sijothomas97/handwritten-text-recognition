# Lean runtime deps for serving only (no torch — inference runs on
# onnxruntime against the exported checkpoints/crnn.onnx). Mirrors the
# relevant subset of requirements.txt/pyproject.toml.
numpy==2.3.1
pillow==11.2.1
onnxruntime==1.22.0
fastapi==0.141.1
uvicorn==0.52.1
python-multipart==0.0.32
