# Pinned runtime deps (mirrors pyproject.toml). Install with:
#   uv venv -p 3.12 .venv && uv pip install -p .venv/bin/python -e ".[dev]"
torch==2.7.1
numpy==2.3.1
pillow==11.2.1
onnx==1.18.0
onnxruntime==1.22.0
fastapi==0.141.1
uvicorn==0.52.1
python-multipart==0.0.32
pytest==8.4.1
httpx==0.28.1
